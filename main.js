 let a=40;
 let b=20;
 const add=require("./add")

 const sub=require("./sub")
 const mul=require("./multiple")
 const div=require("./divide")
 console.log(add(a,b),sub(a,b),mul(a,b),div(a,b))
